# Analyze Library

> Audience: Пользователи этой страницы.
> Type: how-to

UI can run SONARA, MAEST, MERT, CLAP and available CLASSIFIERS. UI `Analyze limit = 0` means whole library; positive values count missing results for selected family. CLI whole-library analysis omits `--limit`. Analysis writes SQLite metadata/features/embeddings only.
