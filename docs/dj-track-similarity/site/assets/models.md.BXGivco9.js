import{c as a,Q as t,j as s,m as d}from"./chunks/framework.DVkeSyfh.js";const m=JSON.parse('{"title":"Analysis Models","description":"","frontmatter":{},"headers":[],"relativePath":"models.md","filePath":"models.md","lastUpdated":1780261007000}'),o={name:"models.md"};function i(r,e,n,l,c,h){return t(),s("div",null,[...e[0]||(e[0]=[d('<h1 id="analysis-models" tabindex="-1">Analysis Models <a class="header-anchor" href="#analysis-models" aria-label="Permalink to &quot;Analysis Models&quot;">​</a></h1><p>This page describes the models behind each analysis family: what each model is, what it produces, and how the app uses and stores its output. Use it together with <a href="./analysis.html">Analysis</a>, which focuses on <em>which</em> pass to run first, and <a href="./search-and-tags.html">Search &amp; Tags</a>, which covers how the results are searched.</p><p>All model analysis is database-first: it reads audio and writes results to SQLite only. It does not modify audio files. The one explicit audio-file write path is MAEST genre tag saving, documented in <a href="./search-and-tags.html">Search &amp; Tags</a>.</p><h2 id="model-summary" tabindex="-1">Model Summary <a class="header-anchor" href="#model-summary" aria-label="Permalink to &quot;Model Summary&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Model</th><th>Identifier / checkpoint</th><th>Output</th><th>Embedding key</th><th>Device</th><th>Needs</th></tr></thead><tbody><tr><td>Sonara</td><td><code>sonara.analyze_file</code></td><td>Explainable playlist features</td><td>n/a</td><td>CPU</td><td><code>sonara</code></td></tr><tr><td>MAEST</td><td><code>discogs-maest-30s-pw-129e-519l</code> (<code>maest-infer</code>)</td><td>Genre labels + embedding</td><td><code>maest</code></td><td><code>auto</code>/<code>cpu</code>/<code>cuda</code></td><td><code>ml</code></td></tr><tr><td>MERT</td><td><code>m-a-p/MERT-v1-95M</code></td><td>Audio-to-audio embedding</td><td><code>mert</code></td><td><code>auto</code>/<code>cpu</code>/<code>cuda</code></td><td><code>ml</code></td></tr><tr><td>CLAP</td><td><code>music_audioset_epoch_15_esc_90.14.pt</code> (LAION)</td><td>Audio + text embeddings</td><td><code>clap</code></td><td><code>auto</code>/<code>cpu</code>/<code>cuda</code></td><td><code>ml</code></td></tr></tbody></table><p>Sonara features live in <code>metadata_json.sonara_features</code>. MERT, MAEST, and CLAP vectors live in the <code>embeddings</code> table under their embedding key. See <a href="./database.html">Database</a> for the full storage layout.</p><p>Device selection for MAEST, MERT, and CLAP follows one rule: <code>auto</code> picks CUDA when PyTorch sees a GPU, otherwise CPU; explicit <code>cuda</code> errors if CUDA is unavailable. These three families also use inference batching through <code>--inference-batch-size</code>; decoded track batching is controlled separately with <code>--track-batch-size</code>. The <code>ml</code> and <code>sonara</code> install groups are described in <a href="./install.html">Install</a>.</p><h2 id="sonara" tabindex="-1">Sonara <a class="header-anchor" href="#sonara" aria-label="Permalink to &quot;Sonara&quot;">​</a></h2><p>Sonara is the fast, explainable feature pass. The shared audio loader starts with <code>sonara.analyze_file</code>, and the app stores a focused set of playlist features in <code>metadata_json.sonara_features</code> plus the model name in <code>metadata_json.sonara_model</code>.</p><p>It produces grouped, human-readable features: core rhythm/loudness/spectral summaries, perceptual values (<code>energy</code>, <code>danceability</code>, <code>valence</code>, <code>acousticness</code>), musical key with confidence, and tonal analysis. Sonara BPM and key are analyzed values, not copied file tags, and the app keeps raw Sonara key data without deriving Camelot notation.</p><p>How the app uses it:</p><ul><li>The SONARA search tab ranks tracks by these features with custom mixer weights and modifiers.</li><li>Library-level fields such as analyzed BPM, key, energy, danceability, and loudness come from Sonara.</li><li>Sonara features are one of the inputs for promoted classifier scoring.</li></ul><p>Sonara runs on CPU and participates in the shared decoded-track batches controlled by <code>track_batch_size</code>; it does not use a neural inference batch. Run Sonara first if you are unsure where to start; it is the most transparent family because the UI can show and mix its feature groups directly. For the full key list, see <a href="./analysis.html">Analysis</a>.</p><h2 id="maest" tabindex="-1">MAEST <a class="header-anchor" href="#maest" aria-label="Permalink to &quot;MAEST&quot;">​</a></h2><p>MAEST predicts genre labels and stores a MAEST embedding. The adapter uses <code>maest-infer</code> with the <code>discogs-maest-30s-pw-129e-519l</code> model.</p><p>It analyzes up to three 30-second windows per track (around a 60-second offset, near 38% of duration, and near 72% of duration). Impossible or duplicate windows are clamped and deduplicated. Per-label activations are averaged across windows to pick the top labels, and the MAEST embedding rows are averaged across the same windows.</p><p>It writes to SQLite only:</p><ul><li><code>metadata_json.maest_model</code></li><li><code>metadata_json.maest_genres</code></li><li><code>metadata_json.maest_syncopated_rhythm</code></li><li>an embedding under embedding key <code>maest</code></li></ul><p>How the app uses it:</p><ul><li>Generated genre labels feed the metadata dialog and the optional genre tag write workflow.</li><li><code>maest_syncopated_rhythm</code> drives the library <code>syncopated</code> preset.</li><li>The MAEST embedding is an input for promoted classifier scoring.</li></ul><p>MAEST analysis itself never modifies audio. Saving labels into standard genre tags is a separate, explicit action covered in <a href="./search-and-tags.html">Search &amp; Tags</a>.</p><h2 id="mert" tabindex="-1">MERT <a class="header-anchor" href="#mert" aria-label="Permalink to &quot;MERT&quot;">​</a></h2><p>MERT builds audio-to-audio embeddings under embedding key <code>mert</code>. The default model is:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>m-a-p/MERT-v1-95M</span></span></code></pre></div><p>How the app uses it:</p><ul><li>The MERT search tab finds tracks close to selected seed tracks in MERT embedding space.</li><li>MERT search uses only MERT vectors; it does not mix Sonara features or CLAP vectors.</li><li>MERT embeddings are an input for promoted classifier scoring.</li></ul><p>Results depend on existing MERT embeddings, so newly scanned tracks must be analyzed before they appear in useful MERT results. Run MERT when seed-track similarity matters more than explainable controls.</p><h2 id="clap" tabindex="-1">CLAP <a class="header-anchor" href="#clap" aria-label="Permalink to &quot;CLAP&quot;">​</a></h2><p>CLAP builds music-focused audio embeddings under embedding key <code>clap</code> and can also embed text into the same space for text-to-audio search. The active checkpoint is:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>lukewys/laion_clap/music_audioset_epoch_15_esc_90.14.pt</span></span></code></pre></div><p>How the app uses it:</p><ul><li>The CLAP text search tab can embed positive and negative prompt variants and rank CLAP audio vectors by adaptive contrast.</li><li>The <code>dj-sim text-search</code> CLI command runs direct single-prompt CLAP search from the terminal.</li><li>Text search requires CLAP audio embeddings produced by the same checkpoint.</li></ul><p>Run CLAP when you want to search by mood, instrumentation, energy, or other descriptive language. Clear, concrete prompts usually work better than a single genre word. In the UI, prompt presets provide local Find/Avoid pairs and fill the Text query and Avoid fields directly.</p><h2 id="promoted-classifiers" tabindex="-1">Promoted Classifiers <a class="header-anchor" href="#promoted-classifiers" aria-label="Permalink to &quot;Promoted Classifiers&quot;">​</a></h2><p>Promoted classifiers are not audio-analysis models; they are local profiles trained in Rhythm Lab that score tracks from stored SONARA, MERT, and MAEST data. In the UI, <code>CLASSIFIERS</code> is part of the same selected-model analysis job: tracks missing classifier scores become candidates. SONARA, MAEST, and MERT must already exist for those tracks or be selected in the same run; otherwise the job returns a clear error instead of silently auto-selecting models. CLAP is not an input for promoted classifiers, but selected CLAP work for each decoded track batch is still completed before classifier scoring runs for that batch. Standalone classifier scoring still reads existing analysis only. See <a href="./analysis.html">Analysis</a> for scoring details and <a href="./rhythm-lab.html">Rhythm Lab</a> for training and promotion.</p>',35)])])}const u=a(o,[["render",i]]);export{m as __pageData,u as default};
