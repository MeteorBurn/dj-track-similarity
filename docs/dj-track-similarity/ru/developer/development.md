# Development

> Audience: Пользователи этой страницы.
> Type: how-to

Install with `python -m pip install -e ".[sonara,ml,rhythm-lab,dev]"`. Prefer `python -m pytest`. Build frontend from `frontend/` and docs from `docs\dj-track-similarity` with `npm run build`. Check ports before starting servers.
