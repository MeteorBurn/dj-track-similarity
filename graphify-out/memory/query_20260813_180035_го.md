---
type: "path_query"
date: "2026-08-13T18:00:35.544046+00:00"
question: "го"
contributor: "graphify"
outcome: "useful"
source_nodes: [".score_row()", "_predict_probabilities()", "_model_class_order()", "_argmax_with_tiebreak()", "_score_bucket_from_score()", "ClassifierScoreRecord", "utc_timestamp()", "ClassifierFeatureRow", "ClassifierScoreWrite", ".__post_init__()"]
---

# Q: го

## Answer

Expanded from original query via graph vocab: [score, row, predict, probabilities, argmax, tiebreak, bucket, classifier, record, write, timestamp, labels]. The production ClassifierScorer.score_row() has direct EXTRACTED call edges to _predict_probabilities(), _argmax_with_tiebreak(), ClassifierScoreRecord, _score_bucket_from_score(), and utc_timestamp(); it references ClassifierFeatureRow and ClassifierScoreWrite. _predict_probabilities() directly calls _model_class_order(), which is also used by _validate_model(). The graph does not contain a direct production edge from _predict_probabilities() to a model.predict_proba() node; its shortest path to the test probability-model method is four hops and includes an INFERRED edge, so the exact model invocation cannot be claimed from graph output alone. ClassifierScorer has an EXTRACTED method edge to score_row(), and ClassifierScoreWrite has an EXTRACTED method edge to its own __post_init__(). Tests confirm deterministic argmax and bucket-boundary behavior, exact-tie resolution by manifest label order, and probability precision preservation. The graph does not expose the numeric bucket thresholds or post-init field checks as nodes, so those values remain outside this graph-only answer.

## Outcome

- Signal: useful

## Source Nodes

- .score_row()
- _predict_probabilities()
- _model_class_order()
- _argmax_with_tiebreak()
- _score_bucket_from_score()
- ClassifierScoreRecord
- utc_timestamp()
- ClassifierFeatureRow
- ClassifierScoreWrite
- .__post_init__()
- test_public_scorer_uses_deterministic_argmax_and_bucket_boundaries()
- test_public_scorer_breaks_exact_ties_by_manifest_label_order()
- test_break_energy_public_scorer_preserves_probability_precision()