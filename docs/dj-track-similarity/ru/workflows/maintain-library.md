# Maintain Library

> Audience: Пользователи этой страницы.
> Type: how-to

Routine maintenance: scan new files, Refresh Tags, run missing analysis, relocation preview before apply. Risky tools: Audio Dedup apply/delete and audio repair `--apply`, both after report/dry-run. Database optimization creates a backup before maintenance PRAGMAs.
