# Smart Set Builder Routing

> Audience: Пользователи этой страницы.
> Type: how-to

SET combines similarity, transition compatibility, BPM/energy preferences, diversity, artist guard and optional classifier modifiers. `general` BPM keeps transition rules; `low_to_high` and `high_to_low` add actual-BPM trajectory. MAEST genre labels are not selection source.
