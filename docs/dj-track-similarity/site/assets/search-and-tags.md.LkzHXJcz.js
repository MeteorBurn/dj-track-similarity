import{c as a,Q as t,j as s,m as o}from"./chunks/framework.DVkeSyfh.js";const u=JSON.parse('{"title":"Search and Tag Writing","description":"","frontmatter":{},"headers":[],"relativePath":"search-and-tags.md","filePath":"search-and-tags.md","lastUpdated":1781981150000}'),r={name:"search-and-tags.md"};function i(d,e,c,n,l,h){return t(),s("div",null,[...e[0]||(e[0]=[o(`<h1 id="search-and-tag-writing" tabindex="-1">Search and Tag Writing <a class="header-anchor" href="#search-and-tag-writing" aria-label="Permalink to &quot;Search and Tag Writing&quot;">​</a></h1><p>This page covers the search tabs, classifier filtering surface, and the explicit genre-tag write workflow. Use it when the library is already scanned and you want to find useful neighbors, build a temporary set, or decide whether to save MAEST genres back to files.</p><h2 id="search-modes" tabindex="-1">Search Modes <a class="header-anchor" href="#search-modes" aria-label="Permalink to &quot;Search Modes&quot;">​</a></h2><p>The search panel has separate tabs.</p><p>Choose the tab by intent:</p><table tabindex="0"><thead><tr><th>Tab</th><th>What it does</th><th>Use when</th></tr></thead><tbody><tr><td>SET</td><td>Generates an ordered Smart Set Builder preview from manual seeds or auto anchors.</td><td>You want a DJ-oriented sequence candidate, not just a single-model search result.</td></tr><tr><td>SONARA</td><td>Searches with explainable playlist features and custom mixer weights.</td><td>You want DJ-transition candidates and control over rhythm, tempo, timbre, dynamics, or harmonic balance.</td></tr><tr><td>MERT</td><td>Searches from selected seed tracks in MERT embedding space.</td><td>You want audio-model similarity without tuning feature weights.</td></tr><tr><td>CLAP</td><td>Searches CLAP audio embeddings from a text prompt.</td><td>You know the sound or mood you want, but do not have a seed track.</td></tr><tr><td>CLASS</td><td>Filters by promoted classifier scores.</td><td>You want a reusable personal signal trained in Rhythm Lab.</td></tr></tbody></table><p>The library browser also has a <code>syncopated</code> preset filter. It selects tracks whose stored MAEST metadata has <code>maest_syncopated_rhythm = true</code> and can be combined with normal library text search and the add-filtered-tracks workflow. Each library row also has a heart button for a local liked-track list. The heart filter in the library controls shows only liked tracks in the same paginated browser, and it can be combined with text search, the syncopated preset, classifier score filters, and add-filtered-tracks. The library controls include an explicit <code>LIKE</code> / <code>FTS</code> search-mode toggle. <code>LIKE</code> is the default substring search across artist, title, album, path, and metadata. <code>FTS</code> uses the token-based SQLite FTS5 index. It can count or narrow token matches much faster, but it does not match arbitrary substrings inside one token and very common tokens can still spend noticeable time sorting the first page by library order. The add-filtered-tracks workflow uses the selected search mode. The library controls also include a sort-direction button that reverses the currently loaded page in the browser without changing the backend query or stored database order. Pagination controls sit on the left side of the library controls after the filter buttons. The right side shows the total count for the current library view followed by sort-direction and add-visible-tracks controls. The total count and page counter reflect the active text search, search mode, preset, liked filter, and classifier score filters.</p><p>The CLASS tab contains classifier controls discovered from promoted <code>models/classifiers/*/model.json</code> metadata. Each promoted classifier can be filtered with a slider, and that slider filters the library and add-filtered-tracks workflow by <code>track_classifier_scores.score</code>. Each classifier row also has its own score button. Use that button after promoting a new Rhythm Lab profile when you want to score only that classifier&#39;s missing rows without deleting or recomputing scores for older promoted classifiers. The main analysis block can still run the <code>CLASSIFIERS</code> checkbox after any selected audio-analysis models. The analysis model rows show coverage counts for SONARA, MAEST, MERT, CLAP, and complete promoted-classifier score coverage; the top header keeps only the total track count.</p><h3 id="set-smart-set-builder" tabindex="-1">SET / Smart Set Builder <a class="header-anchor" href="#set-smart-set-builder" aria-label="Permalink to &quot;SET / Smart Set Builder&quot;">​</a></h3><p>The SET tab calls <code>/api/set-builder/generate</code> and shows an ordered preview that can be added to the current set. It does not replace or append to the set until you use the preview action.</p><p>SET can run from:</p><ul><li>manual seeds: <code>1-5</code> selected seed tracks;</li><li>auto anchors: <code>1-5</code> related feature-complete tracks sampled by the backend.</li></ul><p>The four modes are:</p><ul><li><code>similar_crate</code> (<code>Similar crate - close</code>): stay close to the seed or anchor zone.</li><li><code>weird_adjacent</code> (<code>Weird adjacent - odd</code>): keep relevance but allow stranger neighboring material.</li><li><code>balanced_set</code> (<code>Balanced set - flow</code>): prefer bridge tracks, softer transitions, and less repeated adjacent texture.</li><li><code>discovery</code> (<code>Discovery - wide</code>): reserve more room for lower-confidence candidates that may be worth checking.</li></ul><p>The SET controls are deliberately explicit:</p><ul><li><code>Seed source</code>: <code>Manual - selected</code> keeps the selected seed chips fixed; <code>Auto - random related</code> samples related anchors on every run.</li><li><code>Set mode</code>: chooses the scoring personality listed above.</li><li><code>Track limit</code>: preview length, default <code>24</code>; seed or anchor positions count toward this number.</li><li><code>Auto anchors</code>: number of random related anchors in auto mode, <code>1-5</code>.</li><li><code>Energy curve</code>: <code>Balanced - steady</code>, <code>Warmup - build</code>, <code>Peak - intense</code>, or <code>Wave - rise/fall</code>.</li><li><code>Diversity</code>: <code>0.00-1.00</code>; lower values stay closer to anchors, higher values widen the set while preserving the mode constraints.</li><li>Classifier sliders: <code>Target boost</code>, <code>Avoid cut</code>, <code>Curve start</code>, and <code>Curve end</code> use stored promoted-classifier scores as optional bias signals.</li><li><code>Reset sliders</code>: resets only SET diversity and classifier sliders; seed source, mode, limit, anchor count, and energy curve stay unchanged.</li></ul><p>The builder requires stored SONARA features plus MERT, MAEST, and CLAP audio embeddings. Its SONARA pass uses a broad set of saved features: rhythm/tempo, dynamics, perception, tonal texture, spectral/timbre values, and saved summary statistics for larger arrays such as MFCC and chroma. It uses MAEST embeddings but does not use MAEST genre labels for choosing tracks. Auto mode samples from high-scoring related anchor candidates each time you generate. The sequence itself is also sampled from mode-scored candidates, so rerunning the same controls can produce a different set while still following the selected mode.</p><p>BPM and key affect ordering as soft transition signals. File tags are preferred first, with SONARA BPM/key as fallback when tags are missing. Classifier controls in the SET tab can boost target classifier scores, avoid unwanted scores, or shape a start-to-end mood curve. These controls read stored <code>track_classifier_scores</code>; the SET tab does not launch classifier analysis. The generated sequence also applies an artist spacing guard: tracks by the same known artist are not placed back-to-back, and one artist can appear at most three times in the preview. Manual seed tracks are preserved as <code>seed_anchor</code> items, but they can be interleaved with generated tracks when needed to satisfy that artist spacing rule.</p><p>Each preview row exposes a reason such as <code>seed_anchor</code>, <code>similar_to_seed</code>, <code>bridge</code>, <code>weird_adjacent</code>, <code>discovery</code>, <code>classifier_match</code>, or <code>mood_shift</code>. Hover the score to inspect model scores, SONARA group scores, classifier scores, and transition confidence.</p><h3 id="sonara-search" tabindex="-1">SONARA Search <a class="header-anchor" href="#sonara-search" aria-label="Permalink to &quot;SONARA Search&quot;">​</a></h3><p>SONARA is the primary explainable seed-search path. It sends selected seed tracks, limit, minimum similarity, mixer weights, and modifiers to <code>/api/search/sonara</code>.</p><p>The UI default result limit is <code>10</code>.</p><p>Mixer weights:</p><ul><li><code>timbre</code></li><li><code>rhythm</code></li><li><code>dynamics</code></li><li><code>harmonic</code></li><li><code>tempo</code></li></ul><p>Modifiers:</p><ul><li><code>energy</code></li><li><code>valence</code></li><li><code>acousticness</code></li><li><code>brightness</code></li><li><code>rhythm_density</code></li><li><code>dynamic_range</code></li><li><code>loudness</code></li></ul><p>The backend still accepts preset mode names for compatibility:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>balanced, vibe, sound, dj_transition, custom</span></span></code></pre></div><p>The active UI path uses the custom mixer.</p><p>Start with SONARA when you want explainable results. Increase or reduce mixer weights to make the search lean toward transition fit, rhythmic feel, harmonic similarity, or overall sound.</p><h3 id="mert-search" tabindex="-1">MERT Search <a class="header-anchor" href="#mert-search" aria-label="Permalink to &quot;MERT Search&quot;">​</a></h3><p>MERT seed search sends seed tracks, limit, and optional minimum similarity to <code>/api/search</code>. It ranks tracks in the MERT embedding space.</p><p>The UI and API default result limit is <code>10</code>.</p><p>Use MERT after running <code>dj-sim analyze --models mert</code> or the matching selected model in the UI analysis job. If results are empty or stale, check whether the candidate tracks have MERT embeddings.</p><h3 id="clap-text-search" tabindex="-1">CLAP Text Search <a class="header-anchor" href="#clap-text-search" aria-label="Permalink to &quot;CLAP Text Search&quot;">​</a></h3><p>CLAP text search sends a text prompt, limit, optional minimum similarity, and device to <code>/api/search/text</code>. It ranks CLAP audio vectors against a CLAP text vector. The UI uses adaptive contrast by default: it sends the Text query as a positive CLAP prompt and the optional Avoid field as a negative CLAP prompt, then ranks candidates by <code>positive similarity - negative similarity</code>. This is useful for queries such as vocal presence, ambience, drum feel, or other broad descriptors where a single positive prompt is often too vague.</p><p>The CLAP tab includes prompt presets. Presets provide local Find/Avoid prompt pairs; selecting one immediately fills both Text query and Avoid, then closes the preset menu. The menu also closes when focus moves to another part of the app.</p><p>The UI and API default result limit is <code>10</code>.</p><p>Concrete English prompts usually work best:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Melancholic minimal house with broken drums, warm chords, no vocals</span></span>
<span class="line"><span>Dark hypnotic techno with sparse percussion and deep rolling bass</span></span>
<span class="line"><span>Organic microhouse with soft pads, plucked textures, and spacious mood</span></span></code></pre></div><p>Use CLAP for exploratory digging. Include musical texture, mood, tempo feel, vocal presence, or instrumentation in the prompt instead of relying only on a genre name.</p><h3 id="class-classifiers" tabindex="-1">CLASS / Classifiers <a class="header-anchor" href="#class-classifiers" aria-label="Permalink to &quot;CLASS / Classifiers&quot;">​</a></h3><p>The CLASS tab is for classifier-driven filtering workflows rather than similarity search. It lists promoted classifiers discovered from <code>models/classifiers/*/model.json</code>:</p><ul><li>Each classifier slider filters the library server-side by stored classifier score.</li><li>Each classifier&#39;s play button resets stored scores for that classifier key and then starts a job for that classifier only.</li><li>The metadata dialog shows stored classifier scores below SONARA features.</li></ul><p>Promoted classifiers require a promoted model file and feature-complete tracks. They do not analyze audio directly. Select <code>CLASSIFIERS</code> in the main analysis block to start cancellable classifier jobs after any selected audio models, or use the per-classifier play button in the CLASS tab for a full recalculation of one classifier across all eligible tracks. Run SONARA, MERT, and MAEST first for the tracks you want to score.</p><p>Use CLASS filters after scoring a promoted classifier. A high score means the model thinks the track matches that profile&#39;s positive label; it is a workflow hint, not a guarantee.</p><h2 id="tag-writing" tabindex="-1">Tag Writing <a class="header-anchor" href="#tag-writing" aria-label="Permalink to &quot;Tag Writing&quot;">​</a></h2><p>MAEST genre saving writes one normalized semicolon-separated genre string, for example:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Tech House; Minimal; Techno</span></span></code></pre></div><p>MAEST category prefixes such as <code>Electronic---</code> are stripped before writing.</p><p>Format-specific genre fields:</p><ul><li>MP3, WAV, AIFF ID3 tags: <code>TCON</code></li><li>FLAC and Vorbis-style tags: <code>GENRE</code></li><li>MP4, M4A, ALAC: <code>©gen</code></li></ul><p>WAV genre writing uses Mutagen&#39;s <code>WAVE</code> support, saves the <code>TCON</code> value, and verifies that the saved value can be read afterward. It does not run a custom RIFF repair step. If a WAV write or readback fails, that track is reported as failed while the batch continues.</p><p>Use tag writing only after reviewing MAEST labels in the app. It is intended for saving generated genre labels into the standard genre field; it is not a general metadata editor and should not be used to change title, artist, album, BPM, key, or custom tags.</p>`,54)])])}const m=a(r,[["render",i]]);export{u as __pageData,m as default};
