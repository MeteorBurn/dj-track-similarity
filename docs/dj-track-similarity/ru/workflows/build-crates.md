# Build Crates

> Audience: Пользователи этой страницы.
> Type: how-to

Use one seed per sound, run MERT/SONARA/CLAP, add candidates to current set as a temporary crate, export CSV for review. Use CLASS filters only when labels are trustworthy.
