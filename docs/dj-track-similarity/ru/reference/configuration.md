# Configuration

> Audience: Пользователи этой страницы.
> Type: how-to

Docs source: `docs/dj-track-similarity/`; output: `site/`; VitePress uses `base: "/docs/"` and `outDir: "site"`. Ports: backend `8765`, frontend Vite `5173`, Rhythm Lab `8777`. `ffmpeg` must be on `PATH` or configured with `DJ_TRACK_SIMILARITY_FFMPEG`.
