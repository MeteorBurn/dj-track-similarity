import{c as a,Q as t,j as i,m as r}from"./chunks/framework.DVkeSyfh.js";const u=JSON.parse('{"title":"Project Overview","description":"","frontmatter":{},"headers":[],"relativePath":"overview.md","filePath":"overview.md","lastUpdated":1780000600000}'),o={name:"overview.md"};function s(l,e,n,d,c,p){return t(),i("div",null,[...e[0]||(e[0]=[r('<h1 id="project-overview" tabindex="-1">Project Overview <a class="header-anchor" href="#project-overview" aria-label="Permalink to &quot;Project Overview&quot;">​</a></h1><p>This page covers what <code>dj-track-similarity</code> is, what it does, and which safety boundaries apply to normal app workflows. Read it first if you are deciding whether the project fits your local DJ library workflow.</p><h2 id="project-overview-1" tabindex="-1">Project Overview <a class="header-anchor" href="#project-overview-1" aria-label="Permalink to &quot;Project Overview&quot;">​</a></h2><p><code>dj-track-similarity</code> is a local music-library analysis tool for DJ-set preparation. It scans a folder of audio files, stores metadata and analysis outputs in SQLite, and helps search for tracks that may work near each other in a set.</p><p>The project is a personal and enthusiast tool, not a commercial product and not a formal audio research benchmark. Its practical goal is to make a real local collection easier to inspect, tag, search, and prepare for DJ use.</p><p>The application has two main surfaces:</p><ul><li>A Python backend and CLI installed as <code>dj-sim</code>.</li><li>A React/Vite frontend served by the FastAPI backend.</li></ul><p>Use the CLI when you want repeatable commands, batch work, or quick checks. Use the web UI when you want to browse, listen, compare candidates, tune search controls, export a temporary set, or review metadata before taking action.</p><h2 id="typical-workflow" tabindex="-1">Typical Workflow <a class="header-anchor" href="#typical-workflow" aria-label="Permalink to &quot;Typical Workflow&quot;">​</a></h2><p>The usual path through the app is:</p><ol><li>Choose or create a SQLite database.</li><li>Scan a music folder.</li><li>Run one or more analysis passes: Sonara, MAEST, MERT, or CLAP.</li><li>Browse the paged library, inspect track metadata, and choose seed tracks.</li><li>Search with SONARA, MERT, or CLAP.</li><li>Assemble a temporary set and export it as M3U or CSV.</li><li>Optionally score promoted classifier profiles.</li><li>Optionally write MAEST genres into standard audio genre tags.</li></ol><p>You do not need to run every analysis family. A practical starting point is:</p><ul><li>Run <code>scan</code> first so the library exists in SQLite.</li><li>Run Sonara when you want fast explainable similarity and library feature filters.</li><li>Run MAEST when you want generated genre labels or the <code>syncopated</code> preset.</li><li>Run MERT when you want seed-track audio similarity from embeddings.</li><li>Run CLAP when you want text prompts such as &quot;dark hypnotic techno&quot;.</li><li>Run promoted classifiers only after Sonara, MERT, and MAEST data exist for the tracks you want to score.</li></ul><h2 id="core-features" tabindex="-1">Core Features <a class="header-anchor" href="#core-features" aria-label="Permalink to &quot;Core Features&quot;">​</a></h2><ul><li>Scans local audio folders and stores track rows in SQLite.</li><li>Reads a fixed whitelist of human-relevant Mutagen file tags.</li><li>Refreshes Mutagen tags for already indexed tracks without deleting analysis.</li><li>Relocates stored track paths after moving the same library folder.</li><li>Extracts Sonara playlist features for rhythm, loudness, tonal, perceptual, and spectral inspection.</li><li>Builds MERT audio embeddings for seed-track similarity search.</li><li>Builds CLAP audio embeddings and CLAP text vectors for text-to-audio search.</li><li>Extracts MAEST genre labels and confidence scores.</li><li>Stores MAEST embeddings during genre analysis.</li><li>Scores promoted classifier models from existing SONARA, MERT, and MAEST data.</li><li>Stores analysis status per track for <code>sonara</code>, <code>maest</code>, <code>mert</code>, and <code>clap</code>.</li><li>Stores classifier scores separately from file tags and model metadata.</li><li>Keeps the library browser server-side paginated for large local collections.</li><li>Loads full metadata for one track only when the metadata dialog opens.</li><li>Serves browser previews, starts playback from the preview button, and transcodes AIFF/AIF to temporary seekable WAV files.</li><li>Exports the current temporary set as <code>.m3u</code> or <code>.csv</code>.</li><li>Resets analysis families independently in SQLite.</li><li>Clears the local database after explicit confirmation.</li><li>Writes standard genre tags from stored MAEST labels only when explicitly requested.</li></ul><h2 id="safety-model" tabindex="-1">Safety Model <a class="header-anchor" href="#safety-model" aria-label="Permalink to &quot;Safety Model&quot;">​</a></h2><p>The default application behavior is database-first and read-only for audio files.</p><ul><li>Scanning reads files and writes SQLite rows only.</li><li>RefreshTags rereads file metadata and writes SQLite rows only.</li><li>Sonara, MAEST, MERT, and CLAP analysis read audio and write SQLite analysis outputs only.</li><li>Promoted classifier scoring reads existing SQLite analysis data and writes SQLite classifier rows only. It does not decode or modify audio.</li><li>Search reads SQLite data and does not change audio or the database.</li><li>Browser preview serves audio for the player; AIFF/AIF previews may be transcoded through <code>ffmpeg</code> to temporary seekable WAV files, but source files are not rewritten.</li><li>Export writes playlist/export files only.</li><li>Analysis reset deletes only selected local SQLite analysis outputs.</li><li>Database clear deletes local SQLite track rows, embedding rows, and dependent classifier-score rows only.</li><li>Library relocation updates only stored <code>tracks.path</code> values in SQLite.</li></ul><p>The explicit exception is the genre-save workflow:</p><ul><li><code>/api/tags/genres/apply</code> and <code>/api/tags/genres/jobs</code> can write standard audio genre tags from stored MAEST labels.</li><li>Genre writing must overwrite only the genre field.</li><li>Existing title, artist, album, BPM, key, and other normal tags must remain intact.</li></ul><p>The standalone <code>scripts/audio_repair/repair_audio_metadata.py</code> helper is another explicit exception. It is separate from the app, dry-run by default, and can rewrite repairable audio files only with <code>--apply</code>.</p><h2 id="supported-audio-files" tabindex="-1">Supported Audio Files <a class="header-anchor" href="#supported-audio-files" aria-label="Permalink to &quot;Supported Audio Files&quot;">​</a></h2><p>The main scanner currently indexes these extensions:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>.aif, .aiff, .alac, .flac, .m4a, .mp3, .ogg, .opus, .wav, .wave</span></span></code></pre></div><p>The repair helper supports a broader diagnostic set because it checks container and codec mismatches for more formats. See <a href="./scripts/repair-audio-metadata.html">the audio metadata repair script</a> for that workflow.</p>',25)])])}const f=a(o,[["render",s]]);export{u as __pageData,f as default};
