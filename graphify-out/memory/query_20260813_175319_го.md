---
type: "path_query"
date: "2026-08-13T17:53:19.001275+00:00"
question: "го"
contributor: "graphify"
outcome: "corrected"
correction: "Do not claim a direct _validate_staged_classifier() -> ClassifierScorer.score_row() call from the current graph. The graph proves scorer/input construction plus an explicit rationale to exercise the production scorer, while the method invocation itself is only a structural path through ClassifierFeatureRow."
source_nodes: ["publish_promoted_artifact()", "_validate_staged_classifier()", "load_classifier_requirements()", "require_scoring_compatible_manifest()", "ClassifierRequirements", "ClassifierScorer", "ClassifierFeatureRow", "AnalysisTarget", ".score_row()", "_predict_probabilities()"]
---

# Q: го

## Answer

Expanded from original query via graph vocab: [publish, promoted, artifact, validate, staged, classifier, requirements, scorer, score, row, compatible, integrity]. Direct EXTRACTED route: publish_promoted_artifact() calls _validate_staged_classifier(); validation calls require_scoring_compatible_manifest(), load_classifier_requirements(), and constructs _AvailableOutputRepository, ClassifierScorer, ClassifierFeatureRow, and AnalysisTarget. load_classifier_requirements() validates the scoring-compatible manifest, converts it to ClassifierSpecification, validates feature inputs, requires available AnalysisOutput data, and verifies artifact SHA-256. The rationale says _validate_staged_classifier() exercises the production scorer before a generation becomes current. However, the graph has no direct call edge from _validate_staged_classifier() to the production ClassifierScorer.score_row(); the shortest route is structural through ClassifierFeatureRow. The production ClassifierScorer has an EXTRACTED method edge to score_row(), and score_row() directly calls _predict_probabilities(), _argmax_with_tiebreak(), ClassifierScoreRecord, _score_bucket_from_score(), and utc_timestamp(), returning ClassifierScoreWrite. Publication call locations show staged write at L129, directory fsync L131, staged-pair verification L132, and semantic classifier validation L142; the rationale says the promoted root is replaced only after the staged pair is valid. Tests cover validation before root replacement, semantic-invalid artifacts preserving the old root, root-pair replacement, digest mismatch rejection, deterministic argmax/buckets, and manifest-order tie breaks.

## Outcome

- Signal: corrected
- Correction: Do not claim a direct _validate_staged_classifier() -> ClassifierScorer.score_row() call from the current graph. The graph proves scorer/input construction plus an explicit rationale to exercise the production scorer, while the method invocation itself is only a structural path through ClassifierFeatureRow.

## Source Nodes

- publish_promoted_artifact()
- _validate_staged_classifier()
- load_classifier_requirements()
- require_scoring_compatible_manifest()
- ClassifierRequirements
- ClassifierScorer
- ClassifierFeatureRow
- AnalysisTarget
- .score_row()
- _predict_probabilities()
- _argmax_with_tiebreak()
- _score_bucket_from_score()
- ClassifierScoreWrite
- _verify_staged_pair()
- _write_fsynced()
- _fsync_directory()